//Math é um objeto qe tem propriedades e métodos para constantes e funções matemáticas
const raio = 5.6
const area = Math.PI * Math.pow(raio, 2) // pi x raio ao quadrado
    // Math.pow calcula potências
    // Math.PI já entende 3.14....

console.log(area)
console.log(typeof Math) // object

/*
Math.E // Constante de Euler
Math.PI // Constante PI
Math.sqrt(x) // Retorna a raiz quadrada positiva de um número
Math.trunc(x) // Retorna a parte inteira de x, removendo quaisquer digitos fracionários
Math.pow(x,y) // retorna a base x elevada à potência y, ou seja, x elevado a y
Math.round(x) // retorna o valor arredondado de x, para o valor inteiro mais próximo
Math.sign(x) // retorna o valor de x, indicando se é positivo, negativo ou zero

*/
















