/* propriedades são informações que podem ser verificadas de um valor, quase todos os valores de javascript 
   tem propriedades, menos null e underfined

//============= podemos acessar as propriedades de duas maneiras:===================================*/

let nome = "João";
console.log(nome.length);

let numeros = [1, 2, 3, 4, 5, 6];

console.log(numeros.length);
console.log(numeros[1]);
